# Predmetni projekat - Pravljenje smartcontracta


Komande:

```shell
npm install --save-dev hardhat @nomiclabs/hardhat-waffle ethereum-waffle
chai @nomiclabs/hardhat-ethers ethers
npx hardhat
npx hardat test
npx harhat run scripts/deploy.js --network goerli
```
